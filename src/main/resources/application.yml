server:
 port: 8080
spring:
 datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://localhost:3306/adweb_lab3?characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
  username: root # 用户名；
  password: 123456 # 替换为自己的数据库密码；

